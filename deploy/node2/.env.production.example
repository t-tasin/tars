# T.A.R.S. Node 2 (Muscle) — Environment Variables
# Copy to .env and fill in values before deployment

# ChromaDB token authentication (must match Node 1 config)
CHROMA_AUTH_TOKEN=your-chroma-auth-token-here

# Logging
LOG_LEVEL=INFO
