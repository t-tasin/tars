# 2026-04-21 — Kickoff, Vision Lock

## Session goal
Align on full T.A.R.S. vision. Ship pre-dev docs. Phase 0 starts tomorrow.

## Decisions locked
- **Constraint thesis adopted.** No RAM upgrade. $600 hardware stays.
  Narrative becomes "sovereign compute on two 35W mini-PCs."
- **Local-first routing.** Qwen3-30B-A3B (L1 Brain) + Qwen3-4B-tars (L0
  Reflex, LoRA-fine-tuned on Tasin's writing) as primary. Claude/Gemini
  only on explicit escalation signal.
- **Public dashboard goes live in Phase 3**, not end. Recruiters watch
  the build, not just the finished product. Subdomain: `tars.<domain>`
- **CuratorAgent approach** for Tasin Wiki auto-development. Nothing
  writes to wiki/ without Tasin approval. HC-14 added.
- **Autonomy budget** model — 90% autonomous = READ + WRITE_LOCAL +
  WRITE_SELF. WRITE_WORLD + WRITE_INFRA keep HC-01 approval gate.
- **Persona + tone state machine** required. Voice consistency eval gates
  model swaps (HC-15).
- **Delete fake ChromaDB claim.** Replace with Qdrant. CLAUDE.md v2
  reflects reality.
- **Redis queue bug is Phase 1 priority 1.** Current LPUSH/ZPOPMIN
  mismatch = dead distributed pipeline.

## Deliverables shipped this session
- `CLAUDE.md.new` — rewritten from scratch (v2)
- `docs/FEATURES.md` — 9-phase feature tracker, ~130 feature rows,
  state machine PLANNED → IN_PROGRESS → BUILT → TESTED → SHIPPED
- `docs/vision/day_in_life.md` — product spec as narrative
- `docs/vision/wiki_growth.md` — CuratorAgent design
- `docs/journal/2026-04-21.md` — this entry

## Open questions for Tasin
- Which domain to use for public dashboard? (e.g. `tars.tasin.dev`)
- M-series Mac available for free MLX LoRA training? → saves RunPod cost

## Hardware probe results (2026-04-21 evening)
Both nodes IDENTICAL HP Z2 Mini G3:
- CPU: i7-6700 (Skylake desktop, NOT 7700T — 4C/8T, 65W TDP, 3.4-4.0GHz)
- RAM: **16GB DDR4-2133** (2× 8GB SODIMM, max supported 64GB)
- GPU: NVIDIA Quadro M620 Mobile (Maxwell, 2GB GDDR5, 384 CUDA)
- Storage: Samsung NVMe 256GB (Node 1: 77GB free; Node 2: 58GB free)
- OS: Ubuntu 24.04.4 LTS, kernel 6.8
- Docker 29.3.0 + Compose v5.1.0
- Tailscale meshed (brain 100.94.4.103, muscle 100.119.114.125, MBP 100.106.241.10)
- CPU flags: avx + avx2 + bmi + f16c + fma + sse4_2. NO AVX-512.
- 42 days uptime, idle load <0.1
- Node 2 has 36GB used vs Node 1 17GB → cleanup task added

## Implications (model plan rewritten)
- **Original plan assumed 32GB per node — actual is 16GB. Cut model sizes in half.**
- L0: Qwen3-1.7B (LoRA persona) instead of Qwen3-4B
- L1: Qwen3-8B-Instruct-2507 instead of Qwen3.6-35B-A3B (won't fit)
- Stretch bench: 30B-A3B mmap'd from NVMe — may give 4-8 tok/s
- **Redis + Qdrant relocated to Node 1** (free Node 2 RAM for inference)
- GPU dedicated to **Whisper STT (CUDA)** + optional partial llama layer offload
- LinkedIn narrative gets STRONGER: "$700 of decade-old silicon"
- Docs updated: architecture.md, model_tiers.md, tech_stack.md, deployment.md
- FEATURES.md: P0-11..17 added (probe + node-1 relocation + CUDA install + cleanup)
- FEATURES.md: P2-02..05 model rows updated to new picks
- FEATURES.md: P5-10..11 LoRA base updated to 1.7B

## Next session — Phase 0 Monday
1. `git checkout -b phase-0-preflight`
2. Archive old `CLAUDE.md` → `CLAUDE.md.archive.md`; promote `CLAUDE.md.new` → `CLAUDE.md`
3. Fix `test_telegram_handlers.py` import conflict
4. Fix 2 failing tests (`test_briefing_agent`, `test_gemini_client`)
5. Add Prometheus + Grafana to Node 1 docker-compose
6. Run coverage baseline, commit report
7. Update `FEATURES.md` P0 row statuses → `IN_PROGRESS` / `BUILT`

## Metrics (baseline)
- Backend LOC: ~13k Python
- Worker LOC: ~2k Python
- iOS: 46 Swift files
- Tests: 993 total (983 pass, 2 fail, 8 skipped, 1 import-broken)
- Test coverage: unknown — measure tomorrow

## Mood
Optimistic. Scope is real but phase-gated. Constraint thesis feels honest.
Biggest risk: burnout at week 4-5. Counter: 1 rest day/week, cut Phase 4
scope not Phase 3/5 if behind.

Let's cook.

---

# 2026-04-21 (evening) — Phase 0 Pre-flight Green

Fresh Claude Code session per the phase-per-session strategy. Branch
`phase-0-green` off `main`, seven rows landed, one commit per row, no
pushes yet (HC-02 — Tasin approves push).

## Commits

| SHA | Row | Subject |
|-----|-----|---------|
| `93ab0fc` | P0-01 | test(P0-01): unbreak suite — gemini 2.5 asserts + telegram shim cleanup |
| `e145d66` | P0-10 | refactor(P0-10): excise ChromaDB from backend, worker, deploy |
| `60f9ecf` | P0-03 | chore(P0-03): capture coverage baseline |
| `3d01016` | P0-04 | feat(P0-04): Prometheus + Grafana on Node 1 compose |
| `a5e60c6` | P0-05 | feat(P0-05): expose /metrics via prometheus-fastapi-instrumentator |
| `7d06229` | P0-13 | feat(P0-13): relocate Redis from Node 2 to Node 1 |
| `f60445e` | P0-17 | feat(P0-17): point worker at Node 1 Redis (100.94.4.103) |

## Row evidence

### P0-01 — suite green
- Gemini model asserts 2.0 → 2.5 (prod already on 2.5).
- `sys.modules["telegram"]` shims removed from `test_briefings_api.py`
  and `test_notification_service.py`. `python-telegram-bot==22.6` is a
  real dep; fakes were overwriting it, breaking
  `test_telegram_handlers` collection with `'telegram' is not a package`.
- `test_telegram_handlers.py` patch paths re-prefixed with `src.` to
  match handler imports pinned by commit `666c9fd`.
- Final: backend **991** passed / 8 skipped, worker **20** passed.

### P0-10 — ChromaDB wiped
- Stripped from `backend/src/config.py`, `agents/health_monitor.py`
  (gather + target list + `_check_chromadb` method), `api/health.py`
  (helper + response field + overall-status fallback), every
  `mock_chroma` / `chromadb_url` in tests, `worker/src/config.py`,
  `deploy/node2/docker-compose.yml` (service + volume + envs +
  depends_on), `deploy/node2/.env.production.example`.
- `grep -rn chroma backend/src backend/tests worker/src worker/tests deploy` → 0 matches.

### P0-03 — coverage baseline
- backend **66%** (9322 stmts / 3202 missed), worker **36%** (601 / 386).
- `backend/htmlcov/` + `worker/htmlcov/` gitignored.

### P0-04 — Prom + Graf on Node 1
- `prom/prometheus:v2.55.0` @ :9090 (30d TSDB, scrapes
  `host.docker.internal:8000/metrics`, `host-gateway` alias for the
  systemd-unit backend).
- `grafana/grafana:11.3.0` @ :3000 (admin pwd env, signups off).
- Files: `deploy/node1/prometheus.yml`, `deploy/node1/grafana/provisioning/datasources/prometheus.yml`.
- `env.example` gained `GRAFANA_ADMIN_PASSWORD`.
- Status **BUILT**. TESTED waits on Tasin bringing the stack up on Node 1.

### P0-05 — /metrics endpoint
- `prometheus-fastapi-instrumentator>=7.0` added to backend deps.
- `src/main.py`: `Instrumentator().instrument(app).expose(app, endpoint="/metrics", include_in_schema=False)` between `FastAPI(...)` and `include_router(router)`.
- `tests/test_api/test_metrics.py` — 2 route-inspection tests, no
  TestClient (keeps DB-bound lifespan out of unit tests).
- Full suite post-merge: **993** passed / 8 skipped (matches P0-01 goal count).

### P0-13 — Redis to Node 1
- `deploy/node1/docker-compose.yml`: `redis:7-alpine` added (2gb
  maxmemory, allkeys-lru, appendonly, healthcheck, 2G cap).
- `deploy/node2/docker-compose.yml`: redis service + volume removed;
  worker `REDIS_URL` env default now
  `${REDIS_URL:-redis://100.94.4.103:6379/0}`.
- `backend/src/config.py`: `redis_url` default
  `redis://localhost:6379/0` (backend colocated on Node 1).
- Live cutover (`up -d redis` on Node 1, `down redis` on Node 2, worker
  PING) pending — Tasin flips to TESTED after.

### P0-17 — worker pointed at Node 1
- `worker/src/config.py`: `redis_url`
  `redis://100.119.114.125:6379/0` → `redis://100.94.4.103:6379/0`.
- Config import test + `pytest tests/` both green.

## Final suite
```
backend: 993 passed / 8 skipped
worker:   20 passed
```

## Loose ends

- `/Users/tasin/Desktop/Codebase.nosync/project-tars/env.example` lives
  OUTSIDE this git repo. I edited it for consistency (dropped chroma,
  added `GRAFANA_ADMIN_PASSWORD`, repointed Redis to Node 1) but it is
  **not** committed here. Either move it into `tars/` or commit from
  the outer repo that tracks it.
- `python-multipart` was missing from the backend venv — `pip install`
  fixed it during the session. Already declared in `pyproject.toml`,
  so `pip install -e .[dev]` on a clean checkout will pick it up.
- P0-04 + P0-13 + P0-17 remain **BUILT**, not TESTED. Tasin flips
  TESTED after live Node 1/Node 2 smoke (Grafana login, Prometheus
  `/-/healthy`, worker PINGs Node 1 Redis).

## Next session — Phase 1

Queue refactor. P1-01 `integrations/job_queue.py` with
`ZADD`/`ZPOPMIN` sorted-set pattern per CLAUDE.md pitfall #6, then
P1-02/03 agent migrations, P1-04 E2E test, P1-05/06 chroma removal
follow-up (mostly already gone — confirm no leftover imports). Tag
`v0.1-distributed-real` (P1-07) when green.

## PR

Branch `phase-0-green` is 8 commits ahead of `main` (7 feature + 1
journal). No push yet. Command ready for Tasin:
```
gh pr create --base main --head phase-0-green \
  --title "Phase 0: tests green, ChromaDB gone, metrics + Redis live"
```
