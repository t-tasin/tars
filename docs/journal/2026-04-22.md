# 2026-04-22 — Planning Session Close / Phase 0 Handoff

## What shipped (this planning session, zero code)
- `CLAUDE.md.new` thin rewrite (140 lines) — promoted via mv today
- `docs/FEATURES.md` 9-phase tracker, 140+ rows, state machine
- `docs/vision/{day_in_life,wiki_growth}.md`
- 11 spec files under `docs/`
- `tasks/lessons.md` seed
- Hardware probe P0-11 → BUILT
- Docs rewritten to match real hardware P0-12 → BUILT

## Hardware baseline (verified tonight)
| | Node 1 brain | Node 2 muscle |
|---|---|---|
| CPU | i7-6700 4C/8T | i7-6700 4C/8T |
| RAM | 16GB DDR4-2133 | 16GB DDR4-2133 |
| GPU | (same M620, unused) | Quadro M620 2GB, driver 535.288.01 |
| Free disk | 81GB | 83GB (post-prune) |
| Thermals idle | 39°C CPU, 53°C GPU | 39°C CPU, 53°C GPU |
| Tailscale | 100.94.4.103 | 100.119.114.125 |

## Unresolved before Phase 0 starts
- [x] Decide CUDA path: **Path A chosen** — CUDA 12.2 runfile installed, nvcc V12.2.140 working
- [ ] `sudo apt-mark hold nvidia-driver-535 nvidia-utils-535 libnvidia-compute-535` (still pending — CRITICAL)
- [x] Promote CLAUDE.md.new → CLAUDE.md (174 lines), archive old (505 lines)
- [ ] Commit + PR of docs to main (branch `docs/kickoff-vision` staged, awaiting push)
- [ ] CUDA smoke test: compile + run sm_50 kernel to confirm driver/toolkit talk

## Late-night additions
- P0-15 CUDA install → **TESTED** (runfile 12.2.2, nvcc 12.2.140, sm_50 smoke passed)
- gcc-13 mismatch caught early — `CUDAHOSTCXX=/usr/bin/g++-12` pin added to ~/.bashrc
- Driver 535 held on Node 2 (prevents accidental Maxwell-drop upgrade)
- 4GB runfile removed; Node 2 disk now 19GB used / 75GB free
- Future llama.cpp / whisper.cpp CUDA builds must use `-DCMAKE_CUDA_HOST_COMPILER=/usr/bin/g++-12 -DCMAKE_CUDA_ARCHITECTURES=50`

## Session model going forward
Tasin will run each phase in a **fresh Claude Code session**. State lives
in files:
- `CLAUDE.md` — rules + index
- `docs/FEATURES.md` — what's done/pending/blocked
- `docs/journal/` — per-day log
- `tasks/lessons.md` — corrections accumulated across sessions

Session kickoff template in `docs/journal/2026-04-22.md` above handoff.

## Recommendation for Phase 0 fresh session
Tasks to tackle in order:
1. P0-01  Fix 2 failing tests + broken telegram_handlers import
2. P0-03  Baseline coverage report
3. P0-04  Prometheus + Grafana on Node 1 docker-compose
4. P0-05  Wire `fastapi-instrumentator`
5. P0-09  Verify CLAUDE.md promoted
6. P0-10  Delete ChromaDB from backend src
7. P0-13  Move Redis + Qdrant to Node 1 compose (defer Qdrant add until Phase 3)
8. P0-14  Install lm-sensors (if not)
9. P0-17  Worker config point at Node 1 Redis

End state after Phase 0: tests green, Grafana running, ChromaDB gone
from code, Redis on Node 1, docs committed.

## Next journal entry
First Phase 0 working session. Claude reads this file + FEATURES.md.
